<div class="row large-centered" >

<div class=" row panel panelLogin small-12 large-4 large-centered small-centered columns  ">

<h2>Cambiar contraseña</h2>

<%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :put }) do |f| %>
  <%= f.hidden_field :reset_password_token %>

  <div><%= f.label :password, "Nueva contraseña" %><br />
  <%= f.password_field :password, :autofocus => true , :class=>"cajaMargen" %></div>

  <div><%= f.label :password_confirmation, "Confirmar contraseña"  %><br />
  <%= f.password_field :password_confirmation , :class=>"cajaMargen" %></div>

  <div><%= f.submit "Cambiar contraseña" , :class=>"button botonGrande" %></div>
<% end %>

<%= render "devise/shared/links" %>
</div>
</div>
