 <tr id="contrato_<%= contrato.id %>" class="topic message_10997527 fondoGris" >
  <td class="who">
     <%= link_to edit_contrato_path(:contrato_id => contrato.id, :proceso_id => @proceso.id) do   %>
              <%= contrato.objeto %> - 
      <% end %>
    
     <%= button_to "Ver", {:controller => "contratos", :action => "edit",:contrato_id => contrato.id, :proceso_id => @proceso.id},:class => "tiny button secondary radius ", :method=>:post  %>

    <%= button_to "Eliminar", contrato, :class => "tiny button alert radius inlineBlock",  method: :delete, data: { confirm: 'Estas Seguro de eliminar este contrato?' } , :remote => true %>

  <td class="when">
    <a href="#"> <%= fechaFormato1 contrato.created_at %> </a>
  </td>
</tr>