 <tr id="contrato_<%= contrato.id %>" class="topic message_10997527 fondoGris" >
  <td class="who">
      <%= link_to edit_contrato_path(contrato, :proceso_id => @proceso.id), :remote => true do  %>
              <%= contrato.objeto %> 
      <% end %>
    
      <%= button_to 'Ver', edit_contrato_path(contrato, :proceso_id => @proceso.id), :remote => true, :id => "new_contrato_link",:class => "tiny button radius " %>
      
        <%= link_to "Eliminar", contrato, :class => "textRojolinks inlineBlock",  method: :delete, data: { confirm: 'Estas Seguro de eliminar este contrato?' } , :remote => true %>

  <td class="when">
    <a href="#"> <%= fechaFormato1 contrato.created_at %> </a>
  </td>
</tr>