<div class="alert-panel medium" data-alert>
      <div class="alert-panel-title">
        Colaboradores
        <a href="#" class="alert-panel close-reveal-modal" onclick="$('#myModalCompartirProceso').foundation ('reveal', 'close')">&times;</a>
      </div>
		 <div class="alert-panel-message" >
	         <h6>Puedes enviar un mensajes a los colaboradores via correo electronico</h6>
	              <hr/>
	              <div id="listaUsuarios">
	                <% if !@proceso.nil? %>
	                	<% @proceso.abogados.each do |usuario| %>
		                <div class="colaborador">
		                	<div class="imagencolaborador">
						    
						      <% if !usuario.foto.url.include? "missing.png" %>
		                           <img src="<%= usuario.foto.url %>" class="alinearbotones effect-circle-32" style="margin-bottom: 34px;"> 
		                      <% else %>
		                        <img src="/img/user.png" title="<%= usuario.email %>" class="effect-circle-32" style="   margin-left: 3px;">
		                      <% end%> 

								<div class="datoscolaborador">
				                    <strong><span class="posicionNombreUsuario "><%= usuario.email %></span></strong>
				                    <span class="posicionNombreUsuario ">(<%= usuario.email %>)</span>
				                   <a id="contactar_<%= usuario.id %>" class="textolinks pointer right" onclick="mostarcontacto(<%= usuario.id %>)" >Contactar
				                    	</a>
			                       <a id="contactarCancelar_<%= usuario.id %>" class="textolinks pointer ocultar right" onclick="ocultarcontacto(<%= usuario.id %>)" >Cancelar
			                       </a>
				                   
				                </div>
							</div>
						    <div id="panelcontacto_<%= usuario.id %>" class="contactoColaborador ocultar">
						    	 <%= render 'procesos/formContactar', :usuario => usuario %>
						    </div>
						</div>
	                	<% end %>
	                <% end %>
	               
	              </div>
	  	   </div>
      
</div>


  