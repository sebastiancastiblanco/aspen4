<h1>Listing control_accesos</h1>

<table>
  <tr>
    <th>Controlacceso</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @control_accesos.each do |control_acceso| %>
  <tr>
    <td><%= control_acceso.controlAcceso_id %></td>
    <td><%= link_to 'Show', control_acceso %></td>
    <td><%= link_to 'Edit', edit_control_acceso_path(control_acceso) %></td>
    <td><%= link_to 'Destroy', control_acceso, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Control acceso', new_control_acceso_path %>
