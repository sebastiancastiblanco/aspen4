<tr class="topic message_10997527 fondoGris" id="participante_<%= participante.id %>">
  <td class="who">
      <%= link_to edit_participante_path(:participante_id => participante.id, :proceso_id => @proceso.id) do  %>
       <strong>
              <%= participante.rol_participante.rol %> - 
        </strong>
        <%= participante.nombre %>  <%= participante.apellido %> 
        <% end %>


   <%= button_to "Ver", {:controller => "participantes", :action => "edit",:participante_id => participante.id, :proceso_id => @proceso.id},:class => "tiny button secondary radius ", :method=>:post  %>

    <%= button_to "Eliminar", participante, :class => "tiny button alert radius inlineBlock",  method: :delete, data: { confirm: 'Estas Seguro de eliminar este participante?' } , :remote => true %>


  <td class="when">
    <a href="#"> <%= fechaFormato1 participante.created_at %> </a>
  </td>
</tr>